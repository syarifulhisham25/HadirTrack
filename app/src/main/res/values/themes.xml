<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.HadirTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
    </style>

    <!-- Normal app theme -->
    <style name="Theme.HadirTrack" parent="Base.Theme.HadirTrack" />

    <!-- Splash screen theme -->
    <style name="Theme.HadirTrack.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#EEE8FF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/hadirtrack_splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.HadirTrack</item>
    </style>
</resources>